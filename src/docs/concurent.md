There are many small differences in the way that processes and threads are handled.

The most important difference is that memory is share between threads but not between processes.

- That means that co
